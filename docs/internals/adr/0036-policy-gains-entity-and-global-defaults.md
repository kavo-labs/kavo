# ADR-0036 — Policy gains entity- and global-scope defaults

**Status:** superseded by [ADR-0037](/internals/adr/0037-policy-collapses-to-a-single-predicate) — the three-scope resolution this ADR added carries forward, but `PolicyNode` is gone; `policy` is now a single plain function at each scope. See ADR-0037 for the current design.
